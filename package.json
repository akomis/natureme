{
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "yarn --cwd frontend dev",
    "build": "yarn --cwd frontend build",
    "dev:sanity": "yarn --cwd backend/sanity dev",
    "deploy:sanity": "yarn --cwd backend/sanity deploy",
    "dev:medusa": "yarn --cwd backend/medusa dev",
    "build:medusa": "yarn --cwd backend/medusa build"
  }
}
